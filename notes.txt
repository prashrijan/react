npm => node package manager
npx => node package executor

create-react-app => utility -> takes too long


vite => bundler
npm create vite@latest 

<></> this is called fragments cause can only return one elements
few library forces to write keep the file to .jsx when returning html elem
keep the components uppercase 


- to inject expression we need to put {}, we cant put javascript code into this
- evaluated expression

- React looks after the UI updation part
- Hooks let you use different react features
- useState Hooks remembers the information like user input 